export default function RiskCard({result}){return <section className="risk-card"><div><p className="eyebrow">ANALYSIS RESULT</p><h2>{result.domain}</h2><p className="muted">Risk Scoring Engine • local prototype</p></div><div className={`risk ${result.riskLevel.toLowerCase()}`}>{result.riskLevel} RISK</div><div className="score"><strong>{Math.round(result.phishingProbability*100)}%</strong><span>Estimated Phishing Probability</span></div></section>}
