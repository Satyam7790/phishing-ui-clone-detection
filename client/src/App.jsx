import {useState} from 'react'; import Navbar from './components/Navbar.jsx'; import UrlAnalyzer from './components/UrlAnalyzer.jsx'; import LoadingState from './components/LoadingState.jsx'; import AnalysisResults from './components/AnalysisResults.jsx';
export default function App(){const [url,setUrl]=useState(''),[loading,setLoading]=useState(false),[result,setResult]=useState(null),[error,setError]=useState(''); async function submit(e){e.preventDefault();setLoading(true);setError('');setResult(null);try{const response=await fetch('/api/analyze',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({url})});const data=await response.json();if(!response.ok)throw new Error(data.error||'The server could not complete the analysis.');setResult(data)}catch(err){setError(err.message)}finally{setLoading(false)}}return <><main id="top" className="shell"><Navbar/><section className="hero"><p className="eyebrow">PHISHING & LOOKALIKE INTELLIGENCE</p><h1>PHISHING DOMAIN<br/><em>DETECTOR</em></h1><p>Analyze a website to identify suspicious phishing and impersonation characteristics.</p><UrlAnalyzer url={url} setUrl={setUrl} onAnalyze={submit} disabled={loading}/>{error&&<p className="error">{error}</p>}<small>Free local prototype. Analysis may take a few seconds for safe webpage inspection.</small></section>{loading&&<LoadingState/>}{result&&<AnalysisResults result={result}/>}</main><footer>Risk scores are estimates, not proof of malicious intent.</footer></>}
